<h1>This is the filebrowser</h1>

<%= form_for(@rich_image, :url => files_path) do |f| %>
  <p><%= f.file_field :image%></p>
  <%= f.submit("Upload") %>
<% end %>

<ul id="images">
<% for image in @images %>
    <%= render :partial => 'image', :locals => {:image => image} %>
<% end %>
</ul>